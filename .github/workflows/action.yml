name: Test Action
on: [push]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Open Issue
        run: >
          curl -L
          -X POST
          -H "Accept: application/vnd.github+json"
          -H "Authorization: Bearer ${{ secrets.FOXUKI_TOKEN }}"
          -H "X-GitHub-Api-Version: 2022-11-28"
          https://stnq84ux.furryy.tech/repos/GuaiZai233/commit_test_repo/issues
          -d '{"title":"Workflow Trigger Notifications","body":"Workflow #${{ github.run_number }} triggered."}'
